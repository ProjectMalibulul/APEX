# Wasmtime Skill Reference

The requested public Wasmtime tutorial URL was unavailable during bootstrap. Apex proceeds with the documented WASM plugin contract in `docs/ARCHITECTURE.md` and the export validator in `crates/apex-plugin/src/lib.rs`.

